# 论文笔记仓库维护规则

本文件定义当前仓库的有效维护规则。后续 agent、自动化任务或人工新增论文笔记时，应以本文件和 `docs/superpowers/specs/2026-08-05-paper-reading-repository-design.md` 为准。

## 1. 笔记语言

- 从 2026-08-07 起，**新论文笔记统一使用中文总结和分析**，不再要求中英双语。
- 论文原标题、作者姓名、会议/期刊名称、模型名、方法名、数据集名和指标名等专有名词保留原始写法，不强行翻译。
- 已经存在的双语历史笔记无需批量重写，除非用户明确要求。

## 2. 文件组织

- 每篇论文只创建一个 Markdown 文件。
- 文件名格式：`YYYY-short-paper-title.md`。
- 文件名使用小写 ASCII 字符和连字符，不使用空格。
- 论文放入最相关的主主题目录：
  - `papers/3d-human-pose/`
  - `papers/360-vision/`
  - `papers/multiview/`
  - `papers/medical-ai/`
  - `papers/others/`
- 如果论文同时属于多个主题，只保留一个主目录，通过 `tags` 表示其他主题，不复制多份笔记。

## 3. 去重规则

新增笔记前必须检查仓库中是否已经收录同一论文。至少使用以下信息去重：

1. 论文完整标题或核心标题关键词；
2. arXiv ID、DOI 或其他稳定论文标识；
3. 已有笔记中的 paper/project/code 链接。

如果只是被其他笔记正文提及，但没有独立笔记，则不视为已收录。

## 4. 元数据

每篇笔记保留 YAML front matter，至少包含：

- `title`
- `authors`
- `venue`
- `year`
- `reading_date`
- `status`
- `tags`

`status` 只能使用：

- `skimmed`：已浏览结构、核心方法和主要结果；
- `read`：已完整阅读论文；
- `deep-read`：已深入分析、复现或完成系统性实验比较。

不得因为笔记写得较长就自动把状态提高到 `read` 或 `deep-read`。

## 5. 笔记内容

新笔记原则上包含以下中文章节：

1. `基本信息`
2. `一句话总结`
3. `研究问题与动机`
4. `核心方法`
5. `数据集与评价指标`
6. `主要结果`
7. `优点`
8. `局限`
9. `个人评价`
10. `与我的研究关联`
11. `后续阅读`

根据论文实际内容可以增加子章节，但不要为了填模板而编造信息。

## 6. 来源与事实要求

- 优先使用论文原文、arXiv/出版方页面、作者项目主页、官方代码仓库和官方数据集页面。
- 数值、数据集、实验设置、单位、发布日期和 venue 等事实必须能够从可靠来源核验。
- 作者明确声称的结论与自己的判断要区分开。
- 从方法或实验中推导出的判断要明确标注为“推断”“可能”或类似表述，不得包装成论文原结论。
- 不确定的单位、版本、数据或结论不要猜测。
- 不复制论文的大段原文；笔记应使用自己的中文表述进行总结。

## 7. 论文索引

- `PAPER_INDEX.md` 是论文条目的唯一完整索引。
- 每新增、移动、重命名或删除一篇论文笔记，都必须同步更新 `PAPER_INDEX.md`。
- 索引按主题分组；每个主题内部按 `reading_date` 从新到旧排列。
- 索引至少包含：阅读日期、论文年份、论文标题及链接、阅读状态。
- `README.md` 不再维护逐篇论文列表，也不维护完整的 Recently Read 表格。

## 8. README

README 只作为仓库入口，保持简洁，主要包括：

- 仓库简介；
- 研究主题入口；
- `PAPER_INDEX.md` 入口；
- 阅读状态说明；
- 新增笔记方式；
- 维护规则与模板入口；
- License。

不要把新增论文逐篇追加到 README。

## 9. 模板

所有新笔记优先从 `templates/paper-note-template.md` 创建。模板本身使用中文章节和中文说明，YAML 字段名保持稳定，便于后续程序化处理。

## 10. 修改安全

- 修改前先确认远程 `main` 的当前 HEAD，避免覆盖并发更新。
- 一组相关的仓库维护修改尽量使用单一 commit。
- 更新分支时禁止 force push，除非用户明确要求且风险已确认。
